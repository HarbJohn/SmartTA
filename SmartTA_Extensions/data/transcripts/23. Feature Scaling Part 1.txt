So, welcome back. Let's take a look at some techniques that make great-dint-descent work much better. In this video, you see a technique called feature scaling that will enable great-dint-descent to run much faster. Let's start by taking a look at the relationship between the size of a feature that is how big are the numbers for that feature and the size of a associated parameter. As a concrete example, let's predict the price of a host using two features, x1, the size of the host, and x2, the number of bedrooms. Let's say that x1 typically ranges from 300 to 2000 square feet and x2 in the days of set ranges from 0 to 5 bedrooms. So, for this example, x1 takes on a relatively large range of values and x2 takes on a relatively small range of values. Now, let's take an example of a host that has a size of 2000 square feet, has five bedrooms, and a price of 500k or 500k dollars. For this one training example, what do you think are reasonable values for the size of parameters W1 and W2? Well, let's look at one possible set of parameters. Say W1 is 50 and W2 is 0.1 and B is 50 for the purposes of discussion. So, in this case, the estimated price in thousands of dollars is 100,000k here plus 0.5k plus 50k, which is slightly over a hundred million dollars. So, that's clearly very far from the actual price of $500k. And so, this is not a very good set of parameters for W1 and W2. Now, let's take a look at another possibility. Say W1 and W2 were the other way around. W1 is 0.1 and W2 is 50, and B is still also 50. In this choice of W1 and W2, W1 is relatively small and W2 is relatively large, 50 is much bigger than 0.1. So, here, to predict that price is 0.1 times 2000 plus 50 times 5 plus 50. The first term becomes 200k, the second term becomes 250k, and then plus 50. So, this version of the model predicts a price of $500k, which is a much more reasonable estimate, and happens to be the same price as the true price of the house. So, hopefully, you might notice that when a possible range of values of the feature is large, like the size and square feet, which goes all the way up to 2000, it's more likely that a good model will learn to choose a relatively small ramp to value, like 0.1. Likewise, when the possible values of the feature are small, like the number of bedrooms, then a reasonable value for as parameters will be relatively large, like 50. So, how does this relate to gradient descent? Well, let's take a look at the scatter plot of the features, where the size is square feet is the horizontal axis, X1, and the number of bedrooms, X2, is on the vertical axis. If you plot the training data, you notice that the horizontal axis is on a much larger scale, or much larger range of values compared to the vertical axis. Next, let's look at how the construction might look in a contour plot. You might see a contour plot where the horizontal axis has a much narrower range, say between 0 and 1, whereas the vertical axis takes on much larger values, say between 10 and 100. So, the contour is formed ovals or ellipses, and they're shorter one side and longer on the other. And this is because a very small change to W1 can have a very large impact on the estimated price, and does the very large impact on the cost J, because W1 tends to be multiplied by a very large number, the size and square feet. In contrast, it takes a much larger change in W2 in order to change the predictions much, and does small changes in W2? Don't change the cost function nearly as much. So, where does this leave us? This is what my end up happening if you were to run gradient descent, if you were to use your training data as is. Because the contour are so tall and skinny, gradient descent may end up bouncing back and forth for a long time before it can finally find its way to the global minimum. In situations like this, a useful thing to do is to scale the features. This means performing some transformation of your training data so that X1, say, might not range from 0 to 1 and X2 might also range from 0 to 1. So the data points now look more like this, and you might notice that the scale of the plot on the bottom is now quite different than the one on top. The key point is that the rescale X1 and X2 are both now taking comparable ranges of values to each other. And if you run gradient descent on a cost function to find on this rescale X1 and X2 using this transform data, then the contour will look more like this, more like circles and less tall in skinny, and gradient descent can find a much more direct path to the global minimum. So to recap, when you have different features, they take on very different ranges of values, they can cause gradient descent to run slowly. But rescale in the different features so they all take on comparable range of values, can speed up gradient descent significantly. How do you actually do this? Let's take a look at that in the next video.